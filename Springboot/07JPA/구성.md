
---

### ✅ Lend 관계 매핑 정리 예고

#### 1. 엔티티 구성

- `Lend.java` (중간 테이블 역할)
    
- `Book.java` 와의 `@ManyToOne`
    
- `User.java` 와의 `@ManyToOne`
    

#### 2. 관계 매핑 방식

- `@JoinColumn` 사용 여부
    
- `fetch` 타입 (EAGER vs LAZY) 비교 설명 포함
    

#### 3. Repository + 쿼리 메서드

- `findByUser_Username(String username)` 형태
    

#### 4. 테스트 코드

- save, find, 연관 객체 접근 흐름 테스트
    

---
