### 📝 Java 채팅 프로그램 분석

## 📌 주요 개념

- **클라이언트-서버 통신**: `Socket`과 `ServerSocket`을 활용하여 네트워크 기반 채팅 구현
- **스레드 활용**: 클라이언트와 서버의 데이터 송수신을 위해 `Runnable` 인터페이스를 구현하여 별도 스레드로 처리
- **GUI 기반 채팅**: `JFrame`, `JTextArea`, `JTextField` 등을 활용한 GUI 구성
- **멀티 클라이언트 지원**: 서버에서 여러 클라이언트의 메시지를 관리하고 브로드캐스트

---

## 💻 코드 정리

### 1️⃣ **클라이언트 수신 스레드 (`ClientRecvThread.java`)**

```java
public class ClientRecvThread implements Runnable {
    Socket client;
    DataInputStream Din;
    ClientUI gui;

    ClientRecvThread(Socket client, ClientUI ui) {
        this.client = client;
        gui = ui;
    }

    @Override
    public void run() {
        try {
            Din = new DataInputStream(client.getInputStream());
            String recv;
            while (true) {
                recv = Din.readUTF(); // 서버로부터 메시지 수신
                if (recv == null || recv.equals("q")) {
                    gui.area.append("프로그램을 종료합니다");
                    System.exit(-1);
                }
                gui.area.append(recv + "\n");
                gui.area.setCaretPosition(gui.area.getDocument().getLength()); // 스크롤 하단 이동
            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
```

---

### 2️⃣ **클라이언트 UI (`ClientUI.java`)**

```java
public class ClientUI extends JFrame implements KeyListener {
    JTextArea area;
    JTextField txt;
    Socket client;
    String nick;

    ClientUI(String nick) {
        super("Chat Client");
        
        // GUI 구성
        JPanel pan = new JPanel();
        pan.setLayout(null);
        area = new JTextArea();
        txt = new JTextField();
        JScrollPane scroll = new JScrollPane(area);
        scroll.setBounds(10, 10, 260, 300);
        txt.setBounds(10, 320, 260, 30);
        pan.add(txt);
        pan.add(scroll);
        add(pan);

        this.setBounds(100, 100, 600, 400);
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.setVisible(true);

        // 서버 연결
        try {
            client = new Socket("192.168.16.203", 5555);
            ClientRecvThread recv = new ClientRecvThread(client, this);
            new Thread(recv).start();
            new DataOutputStream(client.getOutputStream()).writeUTF(nick);
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("닉네임 입력 : ");
        String nick = sc.nextLine();
        new ClientUI(nick);
    }

    @Override
    public void keyPressed(KeyEvent e) {
        if (e.getKeyCode() == 10) { // Enter 입력 시
            try {
                DataOutputStream Dout = new DataOutputStream(client.getOutputStream());
                Dout.writeUTF("[" + nick + "] : " + txt.getText());
                Dout.flush();
                area.append("[" + nick + "] : " + txt.getText() + "\n");
                txt.setText("");
            } catch (IOException e1) {
                e1.printStackTrace();
            }
        }
    }

    @Override public void keyTyped(KeyEvent e) {}
    @Override public void keyReleased(KeyEvent e) {}
}
```

---

### 3️⃣ **서버 백그라운드 (`ServerBackground.java`)**

```java
public class ServerBackground {
    ServerSocket server;
    ServerUI gui;
    Map<String, DataOutputStream> ClientList = new HashMap<>();

    ServerBackground(ServerUI ui) {
        gui = ui;
        Collections.synchronizedMap(ClientList); // 동기화 처리
    }

    public void Setting() {
        try {
            server = new ServerSocket(5555);
            while (true) {
                gui.area.append("서버 접속 요청 대기중...\n");
                Socket client = server.accept();
                gui.area.append(client.getInetAddress() + " 에서 접속\n");

                ServerRecvThread recv = new ServerRecvThread(client, this, gui);
                new Thread(recv).start();
            }
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    public void addClient(String nick, DataOutputStream Dout) {
        broadCast(nick + " 님이 접속하셨습니다\n");
        ClientList.put(nick, Dout);
    }

    public void removeClient(String nick) {
        gui.area.append(nick + " 님이 퇴장하셨습니다\n");
        ClientList.remove(nick);
    }

    public void broadCast(String msg) {
        for (String key : ClientList.keySet()) {
            try {
                ClientList.get(key).writeUTF(msg);
                ClientList.get(key).flush();
            } catch (Exception e) {}
        }
    }
}
```

---

### 4️⃣ **서버 수신 스레드 (`ServerRecvThread.java`)**

```java
public class ServerRecvThread implements Runnable {
    Socket client;
    DataInputStream Din;
    DataOutputStream Dout;
    ServerBackground background;
    ServerUI gui;
    String nick;

    public ServerRecvThread(Socket client, ServerBackground background, ServerUI gui) {
        this.client = client;
        this.background = background;
        this.gui = gui;

        try {
            Din = new DataInputStream(client.getInputStream());
            Dout = new DataOutputStream(client.getOutputStream());
            nick = Din.readUTF();
            background.addClient(nick, Dout);
        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    @Override
    public void run() {
        try {
            String recv;
            while (true) {
                recv = Din.readUTF();
                if (recv != null) {
                    background.broadCast(nick, recv);
                    gui.area.append(recv + "\n");
                }
            }
        } catch (Exception e) {
            e.printStackTrace();
            background.removeClient(nick);
        }
    }
}
```

---

### 5️⃣ **서버 UI (`ServerUI.java`)**

```java
public class ServerUI extends JFrame implements ActionListener {
    JTextArea area;
    JTextField txt;
    ServerBackground background;

    ServerUI() {
        super("Chat Server");

        JPanel pan = new JPanel();
        pan.setLayout(null);
        area = new JTextArea();
        JScrollPane scroll = new JScrollPane(area);
        scroll.setBounds(10, 10, 550, 550);
        JButton btn = new JButton("인원확인");
        btn.setBounds(580, 10, 100, 30);
        btn.addActionListener(this);

        pan.add(scroll);
        pan.add(btn);
        add(pan);

        this.setBounds(100, 100, 800, 600);
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.setVisible(true);

        background = new ServerBackground(this);
    }

    @Override
    public void actionPerformed(ActionEvent e) {
        int total = background.ClientList.size();
        System.out.println("접속수 : " + total);
    }

    public static void main(String[] args) {
        new ServerUI().background.Setting();
    }
}
```

---

## 🔎 **핵심 정리**

1. **클라이언트-서버 소켓 통신 구현**
2. **멀티 클라이언트 지원**
3. **서버는 클라이언트 연결 관리 (`HashMap`)**
4. **서버-클라이언트 간 데이터 송수신 `DataInputStream/DataOutputStream` 활용**
5. **스레드 활용으로 병렬 처리 (`Runnable`)**
6. **GUI 기반으로 사용자 경험 개선 (`JFrame`)**