```
Exception in thread "main" java.lang.NullPointerException: Cannot invoke "java.util.Collection.toArray()" because "c" is null at java.base/java.util.ArrayList.<init>(ArrayList.java:181) at Stage.<init>(Stage.java:11) at StageInit.<init>(StageInit.java:18) at Test.main(Test.java:10)
```
Null 에러가 떴다.

Enemy 에서 에러인지  확인

```
Enemy test = Enemy.enemyA;
System.out.println("[디버깅] enemyA: " + test); // null이면 로그에 찍힘

Enemy copied = test.copy();
System.out.println("[디버깅] 복사본: " + copied);
```

```
[디버깅] enemyA: enemyA (HP: 100, ATK: 10, DEF: 10)

[디버깅] 복사본: enemyA (HP: 100, ATK: 10, DEF: 10)
```
결과 잘나옴 

```
public Stage(String stagenumber, List<Enemy> enemylist) {
	this.stageNumber = stagenumber;
	this.enemyList = new ArrayList<>(enemyList);
}
```
Stage 에서 생성자 부분이 문제가 된다

```
this.enemyList = (enemyList != null) ? new ArrayList<>(enemyList) : new ArrayList<>();
```
null 일 경우에 빈 리스트로 받게끔 한다.
이것도 문제 없었음

```
public Stage(String stagenumber, List<Enemy> enemylist) {
this.stageNumber = stagenumber;
this.enemyList = (enemyList != null) ? new ArrayList<>(enemyList) : new ArrayList<>();

}
```
생성자에 enemyList list로 오타